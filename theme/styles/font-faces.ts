import { css } from '@emotion/core';

export default css`
    /*
    @font-face {
        font-family: 'Helvetica;
        font-weight: 300;
        font-style: normal;
        font-display: swap;
        src:
            url('${require('path.woff2')}') format('woff2'),
            url('${require('path.woff')}') format('woff');
    }
    */
`;
